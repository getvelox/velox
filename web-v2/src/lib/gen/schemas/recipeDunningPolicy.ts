/**
 * Generated by orval v8.9.0 🍺
 * Do not edit manually.
 * Velox Billing API
 * Open-source usage-based billing engine. Handles pricing, subscriptions,
usage metering, invoice generation, Stripe payments, dunning, and credits.

 * OpenAPI spec version: 2026-04-07
 */
import type { RecipeDunningPolicyFinalInvoiceAction } from './recipeDunningPolicyFinalInvoiceAction';
import type { RecipeDunningPolicyFinalSubscriptionAction } from './recipeDunningPolicyFinalSubscriptionAction';

export interface RecipeDunningPolicy {
  name: string;
  max_retries: number;
  intervals_hours: number[];
  /** What exhausting all retries does to the subscription behind the unpaid invoice. `pause` pauses collection only (the cycle keeps drafting invoices); `cancel` cancels the subscription. No-op on a one-off invoice, which has no subscription.
   */
  final_subscription_action: RecipeDunningPolicyFinalSubscriptionAction;
  /** What exhausting all retries does to the unpaid invoice. `mark_uncollectible` writes it off as bad debt — the receivable closes, the invoice stays on the books for audit, and it remains settleable out of band. `none` leaves it finalized and due, which means it stays open until a human closes or collects it.
  Independent of `final_subscription_action` (ADR-112): the two were one enum until 2026-08-05, which made "cancel and write off" inexpressible.
   */
  final_invoice_action: RecipeDunningPolicyFinalInvoiceAction;
}
