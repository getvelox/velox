; A plain `npm install` fails with ERESOLVE: openapi-typescript's peer
; range excludes the TypeScript 6 this project uses — a known, tolerated
; mismatch (see the 2026-07-26 dependency arc; CI passes the same flag to
; `npm ci`). This makes the documented install command work as written.
legacy-peer-deps=true
